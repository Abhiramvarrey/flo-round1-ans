import re
def contact(num):
    pattern = r'^([+(\s])?(\d{1,3})?[)-.\s]?(\d{3})?[-.\s]?(\d{3,4})[-.\s]?(\d{4})$'
    if re.match(pattern,num):
        print('valid')
    else:
        print('invalid') 
contact(input())
